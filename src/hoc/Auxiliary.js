import React from 'react';


// "children" is a special property
// It ouputs whatever is entered between 
// the opening and closing tag of this component.
const aux = props => props.children;

export default aux;